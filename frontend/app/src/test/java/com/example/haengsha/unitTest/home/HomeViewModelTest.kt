package com.example.haengsha.unitTest.home
/*
import com.example.haengsha.model.viewModel.home.HomeViewModel
import com.example.haengsha.ui.screens.home.EventCardData
import junit.framework.TestCase.assertEquals
import org.junit.Test
import java.time.LocalDate

class HomeViewModelTest {
    private var viewModel = HomeViewModel()

    private val startDate = LocalDate.of(2023, 12, 25)
    private val endDate = LocalDate.of(2023, 12, 31)


    @Test
    fun homeViewModel_updateEventItems_updateIsCorrect() {
        val festivalItems = listOf(
            EventCardData("organizer1", "title1", startDate, endDate, 1, 1, "festival"),
            EventCardData("organizer2", "title2", startDate, startDate, 1, 1, "festival")
        )
        val academicItems = listOf(
            EventCardData("organizer3", "title3", startDate, endDate, 1, 1, "academic"),
            EventCardData("organizer4", "title4", startDate, startDate, 1, 1, "academic")
        )
        TODO NullPointerException occurs
        viewModel.updateEventItems(festivalItems, academicItems)

        assertEquals(viewModel.festivalItems.value, festivalItems)
        assertEquals(viewModel.academicItems.value, academicItems)
    }
}
*/